module.exports = (async () => {
  return {
    transformer: {
      // ⬇️ 주석을 활성화하면 바벨이 활성화됩니다.
      // babelTransformerPath: require.resolve(
      //   "metro-react-native-babel-transformer"
      // ),
    },
  };
})();
